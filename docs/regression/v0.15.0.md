# Rapport de régression — v0.15.0 (STORY-13)

## Périmètre du changement
STORY-13 : écran Sélection Match (nouveau). Aucune fonction `api.js` existante modifiée, aucun fichier partagé touché au-delà de `index.html`/`css/app.css`.

## Passage checklist
Aucune régression plausible identifiée — story purement additive, aucune dépendance modifiée.

## Verdict
**RAS**

## Prochaine étape
STORY-14a (saisie match, boucle cœur) est la dernière pièce manquante avant que le mode Match soit utilisable de bout en bout — c'est la story la plus dense du backlog restant (taille L), à surveiller particulièrement en régression une fois livrée (elle est le premier vrai consommateur de `state.matchCourant`).
