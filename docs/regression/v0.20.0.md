# Rapport de régression — v0.20.0 (STORY-17)

## Périmètre du changement
STORY-17 : simplification du flow "Book par tireur" — l'écran de saisie impact autonome (`screen-impact.js`) est retiré du parcours et du dépôt ; sélectionner ou créer un tireur mène désormais directement au Book. `RESULTAT_OPTIONS`/`resultatLabel()` relocalisés dans `screen-book.js`.

## Mise à jour de la checklist
**Retrait de 2 entrées** (STORY-06a, STORY-06b — "Saisie impact : auto-save 2-3 taps..." et "...anti double-tap...") : ces critères décrivaient exclusivement le comportement de `screen-impact.js`, supprimé intentionnellement par cette story. Ce n'est pas une régression — c'est une fonctionnalité retirée par décision produit (`docs/prd-recentrage-match.md`), la capacité de saisie équivalente reste couverte par les entrées STORY-14a/STORY-14b (mode Match, seul canal de saisie désormais). Retester ces critères n'aurait plus de sens : l'écran qu'ils décrivent n'existe plus par construction.

**Ajout** : 1 entrée STORY-17 (accès direct Book + badge résultat historique).

## Analyse de risque de régression croisée
Deux risques P0 identifiés par le Risk Analyst avant la story, tous deux revérifiés en conditions réelles (E2E) plutôt que par simple lecture :
1. **Book cassé pour tireur avec historique** (badge `resultatLabel` non défini) — testé en direct sur une vraie ligne de production (tireur "11", impact réel `resultat=arret`) : badge "Arrêt" correct, 0 erreur console.
2. **Références résiduelles à l'écran supprimé** — recherche exhaustive de `renderScreen("impact")` / `data-action="back-to-impact"` / `registerScreen("impact"` dans tout `js/` : 0 occurrence.

## Passage checklist (items plausiblement impactés, priorité Critique/Important)
| Feature (checklist) | Méthode | Résultat |
|---|---|---|
| Écran tireur : recherche/récents/création (STORY-04) | Smoke test réel (E2E-17) : recherche "11", recherche "TestE2E17" inexistant, création | RAS |
| Book tireur : stats + historique (STORY-07a) | Smoke test réel (E2E-17) : stats grid et historique affichés correctement pour tireur "11" | RAS |
| Book tireur : heatmaps croisées (STORY-07b) | Smoke test réel (E2E-17) : heatmaps terrain/cage rendues sans erreur pour tireur "11" | RAS |
| Suppression tireur avec confirmation (STORY-15) | Réutilisée comme mécanisme de nettoyage des données de test (E2E-17) : dialogue natif, suppression réelle confirmée | RAS |
| Édition tireur/joueur (STORY-16) | Boutons ✏️ toujours présents et inchangés dans la liste (observé en E2E-17, non ré-exercé en profondeur — hors scope de cette story, aucune ligne touchée) | RAS (smoke visuel) |
| Mode Match — saisie (STORY-14a/14b) | Non touché par le diff de cette story (fichiers distincts) — test de fumée non jugé nécessaire | Non retesté (hors zone plausible de risque) |

Le reste de la checklist (gardien, équipes, joueurs, matchs, sélection match, RLS) est hors zone plausible de risque : aucun fichier partagé avec ces écrans n'a été modifié par STORY-17.

## Verdict
**RAS**
