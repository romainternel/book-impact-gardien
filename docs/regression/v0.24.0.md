# Rapport de régression — v0.24.0 (Corrections Audit Final 2026-09-05)

## Périmètre du changement
Deux corrections ciblées issues de l'Audit Final du 2026-09-05 : validation du gardien mémorisé au démarrage (`main.js`) et ajout d'un header/bouton retour à l'état vide de `screen-joueurs.js`. Aucune story formelle (correctifs directs, cf. `docs/code-review/AUDIT-2026-09-05-corrections.md`).

## Mise à jour de la checklist
**Ajout** : 2 entrées (gardien fantôme détecté au démarrage, cul-de-sac "Joueurs" corrigé).

## Analyse de risque de régression croisée
Le changement le plus large en surface est `main.js`, exécuté à **chaque** démarrage de l'app (pas seulement un écran spécifique) — le risque principal était de casser le démarrage normal (gardien valide) en ajoutant la vérification réseau. Testé explicitement : sélection d'un gardien réel ("Gabin") → démarrage normal, atterrissage sur Accueil, aucun délai perceptible ni régression.

Le changement sur `screen-joueurs.js` est local à cet écran ; le risque était que le déplacement de `bindAppHeader()` avant le retour anticipé casse le câblage de la recherche/liste dans le cas normal (équipe sélectionnée) — testé explicitement, aucune régression.

## Passage checklist (items plausiblement impactés, priorité Critique/Important)
| Feature (checklist) | Méthode | Résultat |
|---|---|---|
| Écran gardien : liste/création/sélection/persistance localStorage (STORY-03) | Sélection d'un gardien réel après le changement de `main.js` → démarrage normal identique | RAS |
| Paramètres → Joueurs (CRUD, STORY-11) | Flux normal Équipes → tap équipe → Joueurs retesté après le changement d'ordre de `onMountScreenJoueurs()` : recherche, ✏️, 🗑 tous fonctionnels | RAS |

Le reste de la checklist est hors zone plausible de risque : aucun autre fichier partagé n'a été modifié.

## Verdict
**RAS**
