# Rapport de régression — v0.22.0 (STORY-18b)

## Périmètre du changement
STORY-18b : habillage visuel réaliste du but (poteaux, barre, filet) et du terrain (marge, dégradé, vignette) sur l'écran de saisie match. Feature purement décorative — aucune structure de données, aucune fonction vendor modifiée, aucun changement de la logique de sauvegarde/verrouillage.

## Mise à jour de la checklist
**Ajout** : 1 entrée STORY-18b (habillage cage/terrain sans régression du hit-testing).

## Analyse de risque de régression croisée
Le risque principal identifié par le Risk Analyst (P1-#4, régression du hit-testing terrain suite à l'ajout du padding décoratif) a été testé au niveau de rigueur le plus élevé de tout ce cycle de contrôle : calcul direct des coordonnées écran (transformation SVG → écran via `getScreenCTM()`) pour deux points séparés de seulement 2 unités de viewBox de part et d'autre de la frontière entre les zones concaves `69MC` et `69MG`, confirmant une résolution correcte des deux côtés. Aucune dégradation de précision détectée.

Un second bug (dégradé radial du terrain silencieusement écrasé par une texture ajoutée après lui dans la cascade CSS — défaut déjà présent dans la spec du Visual Crafter elle-même) a été détecté et corrigé par le Code Reviewer, revérifié par le QA et l'E2E Tester.

## Passage checklist (items plausiblement impactés, priorité Critique/Important)
| Feature (checklist) | Méthode | Résultat |
|---|---|---|
| Picker terrain, y compris zones concaves (STORY-05) | Calcul de coordonnées exact à la frontière `69MC`/`69MG` (cf. ci-dessus) — test le plus rigoureux appliqué à cette feature depuis sa création | RAS |
| Saisie Match : cage/terrain/auto-save (STORY-14a/14b) | Cycle complet réel exécuté (cage "HG" → terrain "9MD" → joueur) après l'habillage : impact créé en base, comportement inchangé | RAS |
| Écran Saisie Match : layout responsive 3 paliers (STORY-18a, livrée juste avant) | Capture à 1000px confirmant la cohabitation correcte du layout 18a (colonnes + liserés) et de l'habillage 18b (cage/terrain), aucun conflit visuel | RAS |

Le reste de la checklist est hors zone plausible de risque : aucun fichier partagé avec les autres écrans n'a été modifié par STORY-18b (diff limité à `css/app.css` et `js/screens/screen-saisie-match.js`, tokens nouveaux et non-collisionnants avec ceux de `zones.css`, vérifié).

## Verdict
**RAS** (après correction, pendant ce même cycle de contrôle, du dégradé radial manquant sur le terrain).
