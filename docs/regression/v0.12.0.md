# Rapport de régression — v0.12.0 (STORY-10)

## Périmètre du changement
STORY-10 : écrans Paramètres et Équipes, `getEquipes()`/`createEquipe()` dans `api.js`, `state.equipeCourante`/`state.matchCourant` ajoutés.

## Passage checklist
| Feature | Vérifiée | Résultat |
|---|---|---|
| Écran Accueil (STORY-09) | Non re-testé (aucune dépendance modifiée) | Non applicable |
| Mode Book par tireur | Non re-testé (aucune dépendance modifiée) | Non applicable |
| Paramètres/Équipes (nouveau) | Oui (local) | RAS |

## Verdict
**RAS**

## Note
Session de vérification live prolongée sur plusieurs heures — une limitation de la méthode de rechargement à chaud par `eval()` a été identifiée et documentée (`docs/e2e/E2E-10-parametres-equipes.md`), sans lien avec le code produit. Recommandation méthodologique ajoutée pour les prochaines sessions.
