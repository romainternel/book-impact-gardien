# Rapport de régression — v0.9.0 (STORY-07b)

## Périmètre du changement
STORY-07b : heatmaps croisées ajoutées à l'écran Book (`js/zone-picker.js` étendu, `js/screens/screen-book.js` étendu). Dernière story du backlog MVP — toutes les stories STORY-01 à STORY-07b sont maintenant livrées.

## Passage checklist
Toutes les features de la checklist repassées : squelette, vendor, client Supabase, RLS, gardien, tireur, picker terrain, saisie impact (cœur + robustesse), Book (stats/historique/heatmaps). **RAS sur l'ensemble.**

## Verdict
**RAS**

## Bilan MVP
Les 4 écrans du MVP (docs/prd.md) sont livrés et intégralement contrôlés :
1. Sélection gardien (STORY-03)
2. Sélection/création tireur (STORY-04)
3. Saisie impact — 2-3 taps (STORY-06a/06b)
4. Book tireur — stats, historique, heatmaps croisées (STORY-07a/07b)

Backend Supabase opérationnel avec RLS least-privilege auditée (STORY-02). Déploiement GitHub Pages fonctionnel. Composants `fenix-terrain-zones-export` réutilisés tels quels sur l'ensemble du parcours (diff vide maintenu à chaque story).
