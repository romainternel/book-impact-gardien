# Rapport de régression — v0.17.0 (STORY-14b)

## Périmètre du changement
STORY-14b : couche de robustesse sur l'écran de saisie match (verrouillage, bandeau erreur/confirmation/annulation). Aucun fichier vendor, aucun autre écran touché.

## Passage checklist
Toutes les features de la checklist repassées en revue : squelette, vendor, client Supabase, RLS (MVP + mode Match), gardien, tireur, picker terrain, saisie impact (cœur + robustesse), Book (stats/historique/heatmaps), Accueil, Paramètres/Équipes/Joueurs/Matchs, Sélection Match, Saisie Match (cœur + robustesse). **RAS sur l'ensemble.**

## Verdict
**RAS**

## Bilan Mode Match
Les 8 stories du mode Match (STORY-08 → STORY-14b) sont livrées et intégralement contrôlées :
1. Schéma étendu (équipes, joueurs, matchs, résultat simplifié)
2. Accueil (choix du mode)
3. Paramètres → Équipes → Joueurs (CRUD complet, refactor anti-duplication)
4. Matchs (CRUD, embedding double-FK)
5. Sélection Match → Saisie Match (cœur + robustesse)

Le mode Match enrichit le Book existant sans le casser (décision produit centrale, vérifiée à chaque étape). Le projet compte maintenant 17 stories livrées au total (MVP + extension Mode Match), toutes avec le même niveau de rigueur de contrôle.
