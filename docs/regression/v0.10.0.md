# Rapport de régression — v0.10.0 (STORY-08)

## Périmètre du changement
STORY-08 : extension du schéma Supabase pour le mode Match (`equipes`, `matchs`, `tireurs.equipe_id`, `tireurs.poste` +`gardien_but`, `impacts.match_id`, `impacts.resultat` +`non_but`, `zone_cage_coherente` étendue). Aucun fichier JS/CSS touché — migration SQL uniquement.

## Passage checklist
Toutes les features front-end de la checklist non re-testées à ce stade (aucun code applicatif modifié) — seule la partie schéma/RLS a été vérifiée, avec un test de régression ciblé et concluant :
- Un tireur "à l'ancienne" (sans `equipe_id`, poste parmi les 6 valeurs déjà existantes) s'insère toujours normalement.
- Un impact "à l'ancienne" (résultat `but`, sans `match_id`) s'insère toujours normalement.

## Verdict
**RAS**

## Note
Nouvelles données de test créées pendant cette vérification (`__TEST_STORY08_EQUIPE_A__`, `__TEST_STORY08_EQUIPE_B__`, `__TEST_STORY08_JOUEUR__`, un match J03, un gardien et un tireur de régression) — `equipes`/`matchs`/`gardiens`/`tireurs` n'autorisant pas `delete` via l'app par design, nettoyage à faire via SQL editor par l'utilisateur (impacts déjà nettoyés via l'app).
