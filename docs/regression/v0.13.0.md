# Rapport de régression — v0.13.0 (STORY-11)

## Périmètre du changement
STORY-11 : nouveau `tireur-form-shared.js`, refactor de `screen-tireur.js` (comportement inchangé, code réorganisé), nouveau `screen-joueurs.js`, `createTireur()` étendu (rétrocompatible), `getJoueursByEquipe()` ajoutée.

## Analyse de risque de régression croisée
Le refactor touche `screen-tireur.js`, dont dépend directement `screen-impact.js` (navigation post-sélection/création) et indirectement `screen-book.js` (les tireurs créés y apparaissent). Ni `screen-impact.js` ni `screen-book.js` n'ont été modifiés dans cette story — le seul point de contact est `createTireur()`, dont la signature reste rétrocompatible (`equipe_id` optionnel, défaut `null`).

## Passage checklist
| Feature | Vérifiée | Résultat |
|---|---|---|
| Recherche/création tireur libre (mode Book) | Oui | RAS — `equipe_id: null` confirmé, comportement identique à avant le refactor |
| Navigation tireur → saisie impact | Oui | RAS |
| Écran Joueurs (nouveau) | Oui | RAS |
| Écran Book (stats/heatmaps) | Non re-testé (aucun fichier modifié, aucune dépendance touchée) | Non applicable — risque jugé négligeable |

## Verdict
**RAS**
