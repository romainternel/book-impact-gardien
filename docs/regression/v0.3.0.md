# Rapport de régression — v0.3.0 (STORY-02)

## Périmètre du changement
STORY-02 : schéma Supabase (3 tables + RLS), `config.js`/`supabase-client.js`/`api.js`, ajout de 4 balises `<script>` dans `index.html` (CDN supabase-js + les trois nouveaux fichiers).

## Passage checklist
| Feature | Vérifiée | Résultat |
|---|---|---|
| Squelette SPA se charge sans erreur bloquante | Oui | RAS — re-vérifié en live après déploiement |
| Fichiers vendor identiques à l'export | Oui | RAS |
| Picker terrain : clic résout la bonne zone | Non re-testé sur cette version (aucun changement dans `zone-picker.js` ou ses dépendances) | Non applicable |
| Client Supabase live opérationnel (nouveau) | Oui | RAS — `getGardiens()` répond correctement depuis `https://romainternel.github.io/book-impact-gardien/` |
| Policies RLS least-privilege (nouveau) | Oui | RAS — voir `docs/security/story-02-supabase-rls.md`, aucun finding Critique/Majeur |

## Verdict
**RAS**

## Note
Une ligne de test (`__TEST_STORY02_A_SUPPRIMER__`) reste en base `gardiens`/`tireurs` en attendant le nettoyage manuel par l'utilisateur (policies RLS n'autorisant pas le `delete` anon sur ces deux tables, par design). Ne pas confondre avec un vrai gardien lors du développement de STORY-03.
